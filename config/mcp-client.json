{
  "//": "Ready-to-paste MCP client registration for the microservices knowledge base.",
  "//stdio": "Use this block for stdio clients (Claude Desktop, VS Code MCP, Cursor).",
  "mcpServers": {
    "microservices-kb": {
      "command": "mcp-kb-server",
      "args": [],
      "env": {
        "MCP_KB_TRANSPORT": "stdio",
        "MCP_KB_REPOS_ROOT": "C:/work/rx/repos",
        "QDRANT_URL": "http://localhost:6333",
        "EMBEDDING_PROVIDER": "fastembed",
        "POSTGRES_ENABLED": "false",
        "GRAPH_BACKEND": "neo4j",
        "NEO4J_URI": "bolt://localhost:7687",
        "NEO4J_AUTH_ENABLED": "false"
      }
    }
  },

  "//http": "For HTTP transport instead, start the server with MCP_KB_TRANSPORT=http and register the URL below.",
  "mcpServersHttpExample": {
    "microservices-kb-http": {
      "url": "http://127.0.0.1:8000/mcp",
      "transport": "http"
    }
  }
}

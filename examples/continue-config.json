{
  "//": "Continue plugin (VS Code or JetBrains) — merge into ~/.continue/config.json.",
  "//note": "Continue can drive Claude/GPT models and connect to MCP servers. Replace paths.",
  "mcpServers": {
    "microservices-kb": {
      "command": "C:/path/to/mcp-microservices-kb/.venv/Scripts/mcp-kb-server.exe",
      "env": {
        "MCP_KB_REPOS_ROOT": "C:/path/to/mcp-microservices-kb/data/repos",
        "QDRANT_URL": "http://localhost:6333",
        "EMBEDDING_PROVIDER": "fastembed",
        "POSTGRES_ENABLED": "false"
      }
    }
  }
}
